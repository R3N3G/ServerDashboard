version: '3.9'

services:
  systemo:
    container_name: systemo
    restart: unless-stopped
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - REACT_APP_SITE_URL=https://sys.tp.unjx.de
    ports:
      - "127.0.0.1:4000:4000"
